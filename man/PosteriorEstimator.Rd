% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimators.R
\name{PosteriorEstimator}
\alias{PosteriorEstimator}
\title{PosteriorEstimator}
\usage{
PosteriorEstimator(...)
}
\arguments{
\item{...}{arguments passed to the Julia version of \href{https://msainsburydale.github.io/NeuralEstimators.jl/dev/API/estimators#Posterior-estimators}{\code{PosteriorEstimator()}}}
}
\value{
a \code{PosteriorEstimator}
}
\description{
Construct a neural posterior estimator. The neural-network
architecture is still defined in Julia (e.g. via \code{juliaEval()}); this
function wraps that architecture in a \code{PosteriorEstimator}.

Typical constructors, matching the Julia methods:
\itemize{
\item \code{PosteriorEstimator(num_parameters, summary_network, num_summaries = ...)}: builds the approximate distribution internally (default \code{q = "NormalisingFlow"}).
\item \code{PosteriorEstimator(summary_network, q)}: an explicit approximate distribution \code{q}.
}

The argument \code{q} may be a Julia type or a string naming one (e.g. \code{"Gaussian"}, \code{"GaussianMixture"}, \code{"NormalisingFlow"}).
}
\seealso{
\code{\link[=PointEstimator]{PointEstimator()}}, \code{\link[=RatioEstimator]{RatioEstimator()}}, \code{\link[=train]{train()}}, \code{\link[=infer]{infer()}}
}
